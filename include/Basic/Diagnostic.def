#ifndef DIAG
#define DIAG(LEVEL, ID, TEXT)
#endif

DIAG(error, lex_unterminated_string, "unterminated string")
DIAG(error, lex_unterminated_regex, "unterminated regex")
DIAG(error, parse_unexpected_token, "unexpected token; expected '%', got '%'")
DIAG(error, sema_invalid_operand_type, "invalid operand type for '%': '%'")
DIAG(error, sema_invalid_operand_types,
     "invalid operand types for '%': '%' and '%'")
DIAG(error, sema_primitive_subscript,
     "cannot use subscript operator on primitive '%'")

#undef DIAG